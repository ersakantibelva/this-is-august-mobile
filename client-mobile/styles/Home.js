import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  errorContainer: {
    flex: 14,
    alignItems: "center",
    justifyContent: "center",
  },
  textError: {
    color: "gray",
    fontSize: 20,
    fontWeight: 'bold'
  },
  textGoBack: {
    color: "gray",
    fontSize: 14,
  }
});
